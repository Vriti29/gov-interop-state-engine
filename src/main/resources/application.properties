# JWT Secret & Expiration
app.jwt.secret=${APP_JWT_SECRET:your_base64_256bit_secret_key_here}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}

# Gateway Integration
gateway.base-url=${GATEWAY_BASE_URL:http://localhost:8081}
gateway.department-id=${GATEWAY_DEPARTMENT_ID:HIGHER_EDUCATION}
gateway.api-key=${GATEWAY_API_KEY:your_gateway_api_key}
gateway.secret=${GATEWAY_SECRET:your_gateway_secret}
