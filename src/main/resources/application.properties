server.port=${PORT:8080}

# ===============================
# Supabase PostgreSQL Configuration
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.sclzhbarirzpyxcdhyze.supabase.co:5432/postgres}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:<pass>}

# ===============================
# JPA / Hibernate Settings
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# JWT Secret & Expiration
# ===============================
app.jwt.secret=${APP_JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}

# ===============================
# Gateway Integration (Contract Frozen)
# ===============================
gateway.base-url=${GATEWAY_BASE_URL:https://interoperability-hub-sih.onrender.com}
# Contract Fix: Changed SCHOLARSHIP -> HIGHER_EDUCATION to match M4 & Policy
gateway.department-id=${GATEWAY_DEPARTMENT_ID:HIGHER_EDUCATION}
gateway.api-key=${GATEWAY_API_KEY:scholarship-secret-key-123}
gateway.secret=${GATEWAY_SECRET:scholarship-secret-key-123}
